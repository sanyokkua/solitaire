# Tasks

> Conventions for every task below: commands are run with the `rtk` wrapper per `AGENTS.md`;
> `*.test.ts(x)` files are Vitest, `*.spec.ts` files are Playwright; tests live in the top-level
> `tests/` tree mirroring `src/`, never colocated. Requirement names in **Implements** refer to
> `specs/tooling/repository-foundation/spec.md` (T) and `specs/app/application-shell/spec.md` (A).

## 1. Build spine

- [x] 1.1 Create the npm project with pinned dependencies and the script surface, and verify a clean install succeeds and every declared script resolves
    - **Implements:** T "Reproducible install on a pinned runtime"
    - **Files:** `package.json`, `package-lock.json`, `.gitignore` (add `playwright-report/`, `test-results/`, `.husky/_/`)
    - **Details:** `private: true`, `type: "module"`, `engines.node: ">=22.22.2"`, exact version pins with no range specifiers. Runtime deps: `react`, `react-dom`, `@reduxjs/toolkit`, `react-redux`. Dev deps: `vite`, `@vitejs/plugin-react`, `typescript`, `eslint`, `@eslint/js`, `typescript-eslint`, `eslint-config-prettier`, `eslint-plugin-react-hooks`, `eslint-plugin-react-refresh`, `prettier`, `vitest`, `@vitest/coverage-v8`, `jsdom`, `@testing-library/react`, `@testing-library/user-event`, `@testing-library/jest-dom`, `@playwright/test`, `husky`, `lint-staged`, `@types/node`, `@types/react`, `@types/react-dom`. Scripts: `dev`, `dev-network`, `build`, `preview`, `format`, `format:check`, `lint`, `lint:fix`, `typecheck`, `test`, `test:unit`, `test:coverage`, `e2e`, `e2e:headed`, `prepare`, and `validate` = `format:check && lint && typecheck && test:unit && build`. Take `/Users/ok/Development/GitHub/minesweeper/package.json` as the version baseline but confirm each pin against the current registry release, per `phased-design.md` §1 ("refresh at Phase 1"). Do **not** add `vite-plugin-pwa` (Phase 8).
    - **Verify:** `rtk npm ci` completes without rewriting `package-lock.json`, and `rtk npm run` lists every script above
    - **Note:** `prepare` is wired to husky in task 6.1; until then it may be absent or a no-op

- [x] 1.2 Add the TypeScript project-reference configuration at the strictness level design D4 requires, and verify the type-check command passes and rejects an unchecked index read
    - **Implements:** T "Strict type checking"
    - **Files:** `tsconfig.json`, `tsconfig.app.json`, `tsconfig.node.json`, `src/vite-env.d.ts`, `vite.config.ts` (pulled forward from 1.3, see Note)
    - **Details:** Root delegates to an app project (`include: ["src", "tests"]`) and a node project (`include` the config files and, later, `scripts`). Beyond `strict`: `noUnusedLocals`, `noUnusedParameters`, `noUncheckedIndexedAccess`, `exactOptionalPropertyTypes`, `noImplicitOverride`, `noFallthroughCasesInSwitch`, `verbatimModuleSyntax`, `isolatedModules`, `noEmit`, `jsx: react-jsx`, `moduleResolution: Bundler`, types `vitest/globals`, `vite/client`, `node`. `src/vite-env.d.ts` declares `__APP_BUILD_TIMESTAMP__: string`. Per design D6/risk list, confirmed empirically that `tsc -b` works with the sibling's `composite` shape (`composite: true` only on the node project) — no latent misconfiguration.
    - **Note:** the node project's `include` (`vite.config.ts`, `vitest.config.ts`, `playwright.config.ts`) resolves to zero files until at least one of those exists — `tsc -b` hard-errors (`TS18003`) on a referenced project with no inputs, confirmed empirically with both literal filenames and globs. Rather than leave the checkbox unchecked until task 1.3, a minimal real `vite.config.ts` (`defineConfig({ plugins: [react()] })`, no `base`/`define` yet) was created here so the node project has a genuine input — not a stub, since it is exactly what `vite build`/`vite dev` need to run today. Task 1.3 extends this same file with `base: '/solitaire/'` and the `__APP_BUILD_TIMESTAMP__` define rather than creating it fresh.
    - **Verify:** `rtk npm run typecheck` exits zero (confirmed); temporarily reading an array element by index and using it without a guard makes it exit non-zero (confirmed — `TS18048`, probe reverted)

- [x] 1.3 Add the Vite build configuration and the application entry point, and verify the build emits a `/solitaire/`-based artifact
    - **Implements:** T "Production build under the Pages base path" (KS-GEN-01)
    - **Files:** `vite.config.ts`, `index.html`, `src/main.tsx`, `src/App.tsx`
    - **Details:** `base: '/solitaire/'`, `define.__APP_BUILD_TIMESTAMP__` from `process.env.BUILD_TIMESTAMP ?? 'dev version'`, `@vitejs/plugin-react`. `index.html` carries `viewport-fit=cover` and no manifest link yet (Phase 8) and no external URL of any kind. `src/main.tsx` mounts `App` inside `StrictMode` and the Redux `Provider` — the store arrives in 4.1, so this task may land `App` as a minimal named-export component and 4.3 gives it routing. Use named exports throughout, matching the sibling project's convention.
    - **Note:** `vite.config.ts` already exists from task 1.2 (`defineConfig({ plugins: [react()] })`, no `base`/`define`) so the TypeScript node project would have a real input file — this task extends it with `base` and `define` rather than creating it.
    - **Verify:** `rtk npm run build` exits zero and every asset URL in `dist/index.html` begins with `/solitaire/`; `grep -E "https?://" dist/index.html` finds nothing

## 2. Code quality tooling

- [x] 2.1 Configure Prettier to the project house style and verify the whole tree checks clean while the spec pack is untouched
    - **Implements:** T "Deterministic source formatting"; design decision D1
    - **Files:** `.prettierrc.json`, `.prettierignore`
    - **Details:** `{ "semi": true, "singleQuote": true, "trailingComma": "all", "printWidth": 120, "tabWidth": 4 }` — note `semi: true`, which reverses the rule still written in `phased-design.md` §1, `AGENTS.md` and `openspec/config.yaml`; task 8.2 corrects those documents and must not be skipped. Ignore `node_modules/`, `dist/`, `coverage/`, `playwright-report/`, `test-results/`, `package-lock.json`, `docs/`, `openspec/`, `.agents/`, `.claude/`, `.husky/_/`.
    - **Verify:** `rtk npm run format` then `rtk npm run format:check` exits zero, and `git status --porcelain docs/ openspec/` is empty afterwards

- [x] 2.2 Configure ESLint with type-aware strict rules and verify it is clean and rejects `any` and floating promises
    - **Implements:** T "Static analysis rejects unsound typing"; design decision D4
    - **Files:** `eslint.config.js`, `src/main.tsx`
    - **Details:** Flat config via `tseslint.config()`: global ignores (`dist`, `coverage`, `playwright-report`, `test-results`, `node_modules`), `@eslint/js` recommended, `tseslint.configs.strictTypeChecked` and `stylisticTypeChecked` (not `recommended`), `languageOptions.parserOptions.projectService: true` with `tsconfigRootDir`, `react-hooks` recommended, `react-refresh/only-export-components`, explicit `@typescript-eslint/no-explicit-any: 'error'` and `consistent-type-imports: 'error'`, and `eslint-config-prettier` **last**. Config files and `tests/` may need a relaxed override block — add one only if a concrete rule conflict appears, and record why inline.
    - **Note:** the typed rule sets are wired via `tseslint.config()`'s `extends` + `files: ['**/*.{ts,tsx}']` form rather than a flat top-level spread — spreading `strictTypeChecked`/`stylisticTypeChecked` globally applies type-aware rules to `eslint.config.js` itself (a `.js` file with no project info), which crashes ESLint (`@typescript-eslint/await-thenable` requires type information it can't get). Scoping the typed configs to `**/*.{ts,tsx}` via `extends` is the documented fix and needed no separate override block for config files. Confirmed empirically: a real `strictTypeChecked` rule, `@typescript-eslint/no-non-null-assertion`, flagged the pre-existing `document.getElementById('root')!` in `src/main.tsx` from task 1.3 — fixed with an explicit presence check rather than an assertion, since that is a one-line, mechanical consequence of a rule this task itself specifies, not new behaviour.
    - **Verify:** `rtk npm run lint` exits zero with no warnings (confirmed); a temporary `const probe: any = 1` and a temporary unawaited promise call each make it exit non-zero (confirmed — `@typescript-eslint/no-explicit-any` and `@typescript-eslint/no-floating-promises`, probes reverted); `rtk npm run typecheck` and `rtk npm run format:check` still pass (confirmed)

## 3. Test harness

- [x] 3.1 Configure Vitest with jsdom and Testing Library, and verify the in-process layer runs and excludes Playwright specs
    - **Implements:** T "Test suites separated by execution layer"
    - **Files:** `vitest.config.ts`, `tests/setup.ts`
    - **Details:** `environment: 'jsdom'` with `environmentOptions.jsdom.url = 'http://localhost/solitaire/'`, `setupFiles: ['./tests/setup.ts']`, `globals: true`, `css: true`, `include: ['tests/**/*.{test,spec}.{ts,tsx}']`, `exclude: ['tests/e2e/**/*.spec.ts']`, v8 coverage with `reporter: ['text','html']` and thresholds, excluding `src/main.tsx` and `src/vite-env.d.ts`. `tests/setup.ts` imports `@testing-library/jest-dom/vitest` and installs a `matchMedia` implementation (jsdom has none; this is the one place a test double is unavoidable, per design D2). Set coverage thresholds to what the real Phase 1 code actually achieves once tasks 4.1–4.4 land — revisit this task's numbers at the end of group 4 rather than guessing now.
    - **Note:** confirmed empirically that Vitest exits non-zero ("No test files found") by default when a run matches zero specs, which conflicts with this task's own verify text ("no tests yet is acceptable"). Added `passWithNoTests: true` to `vitest.config.ts` — the documented option for exactly this case — rather than adding a placeholder test file, which design D2 rules out. Coverage thresholds are set to `0` for all four metrics until task 7.4 revisits them against real coverage.
    - **Verify:** `rtk npm run test:unit` runs and reports zero failures (no tests yet is acceptable at this point) — confirmed, exits 0; `rtk npm run test:coverage` produces a report — confirmed, text summary and `coverage/html` report generated; `rtk npm run typecheck`, `rtk npm run lint` and `rtk npm run format:check` still pass — confirmed

## 4. Application shell

- [x] 4.1 Implement the Redux store and the `app` route slice, and verify the store unit test passes
    - **Implements:** A "Two top-level screens" (KS-GEN-02)
    - **Files:** `src/app/store.ts`, `src/app/appSlice.ts`, `src/app/hooks.ts`
    - **Tests:** `tests/unit/app/store.test.ts` — asserts the initial route is `home`; dispatching `setRoute('game')` yields `game`; the exported `RootState`/`AppDispatch` types resolve against the configured store; a selector reads the route from real store state (no mocked store)
    - **Details:** `appSlice` holds only `route: 'home' | 'game'` with a `setRoute` reducer. Typed `useAppDispatch`/`useAppSelector` hooks. Nothing else — sheets, notices and visibility are Phases 6–7.
    - **Verify:** `rtk npm run test:unit -- tests/unit/app/store.test.ts` passes

- [x] 4.2 Add the design tokens, global stylesheet and bundled fonts, and verify the token contract test passes
    - **Implements:** A "Semantic colour tokens for the three palettes" (KS-SET-03), A "Locally bundled typefaces" (KS-PWA-04), T "No third-party runtime hosts"
    - **Files:** `src/ui/styles/tokens.css`, `src/ui/styles/global.css`, `src/assets/fonts/Inter-Variable.woff2`, `src/assets/fonts/PressStart2P-Regular.ttf`, `src/assets/fonts/README.md`
    - **Tests:** `tests/unit/ui/tokens.test.ts` — reads `tokens.css` as text and asserts (a) every colour role in `docs/spec/specification.md` §8.1 has a custom property under `:root`, under the dark selector and under the night-card selector; (b) every `@font-face` `src` is a path relative to `../../assets/fonts/` and no declaration contains `http://` or `https://`; (c) the `--font-ui` and `--font-pixel` tokens exist
    - **Details:** Copy both font binaries and the SIL OFL attribution README from `/Users/ok/Development/GitHub/minesweeper/src/assets/fonts/` (present locally; no download needed). Fonts live under `src/assets/` so Vite fingerprints them, per design D8. Tokens carry the full light, dark and night-card palettes from specification §8.1. `global.css` starts with `@import './tokens.css'` and sets base typography from the tokens only.
    - **Verify:** `rtk npm run test:unit -- tests/unit/ui/tokens.test.ts` passes

- [x] 4.3 Implement the Home and Game screen shells and route-driven rendering, and verify the shell component test passes for pointer and keyboard
    - **Implements:** A "Two top-level screens" (KS-GEN-02), A "Navigation between the two screens" (KS-GEN-02, KS-A11Y-03)
    - **Files:** `src/ui/screens/HomeScreen.tsx`, `src/ui/screens/GameScreen.tsx`, `src/App.tsx` (routing + stylesheet imports)
    - **Tests:** `tests/component/appShell.test.tsx` — Home renders on first mount and Game does not; a pointer click on the start control renders Game; keyboard focus plus Enter on the start control renders Game; the back control returns to Home by both paths; each navigation control has an accessible name. Drag is not asserted and the test states why inline (no draggable object exists in the shell; card dragging arrives with the board in Phase 5), as `openspec/config.yaml` `rules.specs` requires.
    - **Details:** Static screen text is drawn from `docs/spec/specification.md` §3.1 and §3.2; the controls are real `<button>` elements. `App.tsx` imports the stylesheets in fixed order and renders by `route`. No sheets, no modes, no deal — those are Phases 6–7.
    - **Note:** also touched `src/main.tsx`, one file beyond this task's stated list, to wrap `<App />` in a Redux `Provider` (`createAppStore()`). `App.tsx` now calls `useAppSelector` for routing, which requires a `Provider` ancestor to work outside of tests — task 1.3's own note forecast this exact wiring landing "whenever routing arrives" (i.e. here), and it matches the sibling `minesweeper/src/main.tsx` convention. Button copy ("Deal cards", "Back to Home") is drawn from `specification.md` §3.1 and the mockup rather than invented.
    - **Verify:** `rtk npm run test:unit -- tests/component/appShell.test.tsx` passes — confirmed, 6/6 pass; full `rtk npm run test:unit` still passes (103/103); `rtk npm run typecheck`, `rtk npm run lint` and `rtk npm run format:check` all pass; `rtk npm run build` still emits a working `/solitaire/` artifact

- [x] 4.4 Add the build stamp component, and verify it renders the injected timestamp and the development placeholder
    - **Implements:** A "Build identification"
    - **Files:** `src/ui/components/BuildStamp.tsx`, `src/App.tsx` (render it in the shell)
    - **Tests:** `tests/component/buildStamp.test.tsx` — asserts the value of `__APP_BUILD_TIMESTAMP__` is rendered as text with an accessible name, and that the `dev version` placeholder appears when no build timestamp was supplied
    - **Verify:** `rtk npm run test:unit -- tests/component/buildStamp.test.tsx` passes

- [x] 4.5 Create the reserved layer directories with documentation only, and verify no executable code exists in them
    - **Implements:** A "Layers reserved for later phases carry no behaviour"; design decision D2
    - **Files:** `src/domain/README.md`, `src/solver/README.md`, `src/features/README.md`, `src/i18n/README.md`, `src/pwa/README.md`, `tests/README.md`
    - **Details:** Each README states what belongs in that layer (from `phased-design.md` §3.1), which phase fills it, and the import restrictions that apply to it (for `domain` and `solver`: no React, Redux, DOM or storage imports). `tests/README.md` records the `*.test.ts(x)` vs `*.spec.ts` convention and the three test layers. No `.ts` or `.tsx` file is created in any of these directories.
    - **Verify:** `find src/domain src/solver src/features src/i18n src/pwa -name '*.ts' -o -name '*.tsx' | wc -l` reports `0`, and each directory is tracked by git

## 5. End-to-end layer

- [x] 5.1 Configure Playwright with desktop and touch projects and add the smoke spec, and verify it passes on every project
    - **Implements:** T "Test suites separated by execution layer" (browser-layer scenario)
    - **Files:** `playwright.config.ts`, `tests/e2e/smoke.spec.ts`
    - **Tests:** `tests/e2e/smoke.spec.ts` — loads the app at the base URL, asserts the Home shell heading is visible, asserts the page does not scroll (`scrollHeight <= innerHeight`, `scrollWidth <= innerWidth`), navigates to Game and asserts the Game shell heading, then navigates back
    - **Details:** `testDir: './tests/e2e'`, `testMatch: '**/*.spec.ts'`, `baseURL: 'http://127.0.0.1:5173/solitaire/'`, `webServer` building then serving, `forbidOnly`/`retries` keyed off `CI`, trace and screenshot on failure. Projects: `chromium`, `firefox`, `webkit`, plus the touch projects `iphone-17-pro` (WebKit, 402×874), `iphone-14-pro-max` (WebKit, 430×932) and `galaxy-s25` (Chromium, 360×780), each with `hasTouch` and `isMobile`, per `phased-design.md` §5. `webServer.command` uses `npm run build && npm run preview` (not `dev`, which serves unbuilt source) so the smoke spec exercises the actual production artifact, per design.md's stated smoke intent; `--port 5173` is pinned since `vite preview` defaults to `4173`. The `iphone-17-pro`/`iphone-14-pro-max` projects spread the closest built-in `devices[]` preset (for UA, `hasTouch`, `isMobile`, `defaultBrowserType`) and override only `viewport` to the task's exact pixel sizes, since the installed Playwright version's own presets exclude on-device browser chrome from their viewport height; no `Galaxy S25` preset exists in this Playwright version, so `galaxy-s25` is based on `Galaxy S24` (its preset viewport already matches 360×780 exactly).
    - **Note:** on this development machine (macOS 27 beta, build 26A428), headless Firefox failed every launch with `Could not find profile folder` — a confirmed upstream Playwright bug on macOS 27 (microsoft/playwright#42768): the bundled Firefox resolves its app-data directory to the real `~/Library/Application Support/Firefox`, which macOS 27 now TCC-protects. Not a config or spec defect — chromium, webkit and all three touch projects passed immediately, and CI runs on Linux runners where this never applies. Worked around locally for verification only, by exporting `CFFIXED_USER_HOME` to a scratch directory for the `e2e` run (redirects CoreFoundation's resolved home directory, per the upstream issue's documented fix) — nothing under version control changed for this.
    - **Verify:** `rtk npx playwright install --with-deps chromium firefox webkit` then `rtk npm run e2e` passes on all six projects — confirmed, 6/6 pass (Firefox required the `CFFIXED_USER_HOME` workaround above, macOS-27-only); `rtk npm run typecheck`, `rtk npm run lint` and `rtk npm run format:check` all pass

## 6. Local gates

- [x] 6.1 Install husky and lint-staged and add the pre-commit gate, and verify a mis-formatted file is corrected *in the commit* and a type error blocks the commit
    - **Implements:** T "Commit-time gate"; design decision D5
    - **Files:** `package.json` (`prepare: "husky"`, `lint-staged` config), `.husky/pre-commit`
    - **Details:** `lint-staged`: `*.{ts,tsx}` → `prettier --write` then `eslint --fix`; `*.{json,css,html,md,yml,yaml}` → `prettier --write`. lint-staged restages what it rewrites. `.husky/pre-commit` runs `npx lint-staged`, then `npm run typecheck`, then `npm run test:unit` — never Playwright.
    - **Verify:** On a throwaway commit, stage a file written with 2-space indent and no semicolons; after committing, `git show HEAD:<path>` shows 4-space indent and semicolons — assert the **committed blob**, not the working-tree file (design risk list). Then confirm a deliberate type error causes the commit to be rejected. Reset the throwaway commit afterwards.

- [x] 6.2 Add the pre-push gate and verify a failing end-to-end spec blocks the push
    - **Implements:** T "Push-time gate"
    - **Files:** `.husky/pre-push`
    - **Details:** Runs `npm run e2e`.
    - **Verify:** With a temporarily broken assertion in `tests/e2e/smoke.spec.ts`, a push to a throwaway remote ref is rejected; with the assertion restored, it is allowed — confirmed against a local throwaway bare repo (never `origin`): a broken assertion made all 6 Playwright projects fail and `husky - pre-push script failed (code 1)` rejected the push (`error: failed to push some refs`); with the assertion restored, all 6 projects passed and the push to the throwaway remote succeeded. `rtk npm run typecheck`, `rtk npm run lint` and `rtk npm run format:check` still pass

## 7. Continuous integration and delivery

- [x] 7.1 Add the CI workflow, and verify its contract test passes and its steps reproduce the local gate
    - **Implements:** T "Continuous integration on every push and pull request"
    - **Files:** `.github/workflows/ci.yml`
    - **Details:** Triggers on `push` and `pull_request`; top-level `permissions: contents: read`; `BUILD_TIMESTAMP` from the run start time; checkout, Node 22.22.2 with npm cache, `npm ci`, `npm run validate`, `npx playwright install --with-deps chromium firefox webkit`, `npm run e2e`, then upload `playwright-report/` and `test-results/` as an artifact on failure. **Per `AGENTS.md`, web-search the current release of each action and pin it exactly (`vX.Y.Z`) — do not copy the sibling repository's pins.** No `validate:artifact` step (Phase 8, design D6).
    - **Note:** action versions were looked up against current releases (2026-09-22) rather than copied from the sibling repo, per design D7: `actions/checkout@v7.0.1` (matches sibling), `actions/setup-node@v7.0.0` (sibling pins the older `v6.5.0`), `actions/upload-artifact@v7.0.1` (matches sibling). Task 7.3's contract test does not exist yet — it is that task's own deliverable — so it could not be run here; this workflow was written to satisfy its stated assertions (required steps present, every `uses:` pinned to an exact tag) rather than verified against it directly.
    - **Verify:** `rtk npm run validate` reproduces the workflow's gate locally — confirmed, exits 0 (105/105 unit/component tests pass, build succeeds); the workflow file parses as valid YAML — confirmed via `npx js-yaml`; `rtk npm run format:check` and `rtk npm run lint` both pass on the new file

- [x] 7.2 Add the Pages deployment workflow, and verify its permission scoping and contract test
    - **Implements:** T "Deployment to GitHub Pages from the default branch"
    - **Files:** `.github/workflows/pages.yml`
    - **Details:** Triggers on `push: branches: [master]` and `workflow_dispatch`; `concurrency: { group: pages, cancel-in-progress: false }`; top-level `permissions: contents: read`. Build job guarded by `if: github.ref == 'refs/heads/master'`: checkout, Node 22.22.2, `npm ci`, `npm run validate`, `npm run build` with `BUILD_TIMESTAMP`, `upload-pages-artifact` with `path: dist`. Deploy job depends on build and is the **only** job granted `pages: write` and `id-token: write`. Action versions looked up and pinned as in 7.1. No `validate:artifact` step (Phase 8).
    - **Note:** action versions looked up fresh via the GitHub releases API (2026-09-22): `actions/upload-pages-artifact@v5.0.0`, `actions/deploy-pages@v5.0.1`; `actions/checkout@v7.0.1` and `actions/setup-node@v7.0.0` reused from `ci.yml`, looked up the same day. The `deploy` job also carries a standard `environment: { name: github-pages, url: ... }` block so `actions/deploy-pages`'s `page_url` output is surfaced — this doesn't affect permission scoping or any stated requirement. Job-level `permissions` on `deploy` fully replaces the top-level block for that job (not additive), so `deploy` intentionally has no `contents: read` — it doesn't check out the repo, only consumes the artifact `build` uploaded.
    - **Verify:** Task 7.3's contract test asserts the guards, the concurrency group, the job-scoped permissions and the pinned versions — that task doesn't exist yet, so this task was checked instead against everything statically verifiable now: `npx js-yaml .github/workflows/pages.yml` parses — confirmed; `rtk npm run format:check` and `rtk npm run lint` pass with the file included — confirmed; `rtk npm run validate` still passes — confirmed; a manual read-through confirms both triggers, the concurrency group, the top-level `contents: read`, the `master`-ref guard on `build`, `pages`/`id-token: write` present only on `deploy`, no `validate:artifact` step, and every `uses:` pinned to an exact `vX.Y.Z` tag.
    - **Out of scope, record as follow-up:** the live deploy at `https://sanyokkua.github.io/solitaire/` is only observable after this work reaches `master`, which `AGENTS.md` forbids here. Do not mark it verified.

- [x] 7.3 Add the repository configuration contract test, and verify it fails when the base path or a pinned action version drifts
    - **Implements:** T "Base path agreement across configurations", T "Continuous integration…", T "Deployment to GitHub Pages…"
    - **Files:** `tests/unit/repo/configContract.test.ts`
    - **Tests:** Reads `vite.config.ts`, `vitest.config.ts` and `playwright.config.ts` as text and asserts all three carry `/solitaire/`; reads both workflow files and asserts each required step, the `master` ref guard, `concurrency: pages`, `pages: write` and `id-token: write` present **only** in the deploy job, and that every `uses:` is pinned to an exact `vX.Y.Z` tag
    - **Details:** This is the executable form of design D3's configuration-test rationale — the base path is coupled across four files and its breakage is otherwise deploy-time-only. Reads all five files as raw text (no YAML-parsing dependency added, matching the existing `tokens.test.ts` convention); the deploy-job isolation check splits `pages.yml`'s text at the `deploy:` job boundary and asserts `pages: write`/`id-token: write` appear only on the deploy side.
    - **Verify:** `rtk npm run test:unit -- tests/unit/repo/configContract.test.ts` passes — confirmed, 142/142 unit+component tests pass; three probes confirmed and reverted: (1) changing `base` in `vite.config.ts` away from `/solitaire/` fails the base-path test, (2) changing a pinned action ref (`actions/checkout@v7.0.1` → `@v7`) fails the pinned-version test, (3) moving `pages: write`/`id-token: write` from the `deploy` job's permissions into `pages.yml`'s top-level permissions fails the isolation test; `rtk npm run validate` (format:check, lint, typecheck, test:unit, build) passes clean afterward

- [x] 7.4 Revisit the coverage thresholds set in task 3.1 against the real measured coverage, and verify the coverage command passes at the committed thresholds
    - **Implements:** T "Test suites separated by execution layer" (coverage scenario)
    - **Files:** `vitest.config.ts`
    - **Details:** Set thresholds to the coverage the Phase 1 code actually achieves, leaving no headroom that a later phase could silently consume. If a real file is genuinely untestable in-process, exclude it explicitly and record why, rather than lowering a global threshold.
    - **Note:** measured coverage (via a `json-summary` reporter run) is 100% on all four metrics across every real file (`App.tsx`, `appSlice.ts`, `hooks.ts`, `store.ts`, `BuildStamp.tsx`, `GameScreen.tsx`, `HomeScreen.tsx` — 20/20 statements, 4/4 branches, 9/9 functions, 20/20 lines); no file is genuinely untestable beyond the `main.tsx`/`vite-env.d.ts` exclusion already made in task 3.1. Rather than pin thresholds to that literal 100%, thresholds were set to a flat **80%** floor for all four metrics, a deliberate deviation discussed with and chosen by the project owner: pinning to the exact currently-achieved value risks later phases writing tests just to satisfy the gate rather than to cover real behaviour, and an 80% floor still catches a real regression while leaving room for legitimately hard-to-cover branches (defensive guards, error paths) without gaming pressure.
    - **Verify:** `rtk npm run test:coverage` exits zero at the 80% thresholds — confirmed (actual coverage is 100%, well above the floor); lowering real coverage makes it exit non-zero — confirmed by temporarily skipping `tests/component/appShell.test.tsx` (`describe.skip`), which dropped coverage to 60% statements / 50% branches / 44.44% functions / 60% lines and made `rtk npm run test:coverage` exit 1 with four threshold errors; probe reverted. `rtk npm run validate` (format:check, lint, typecheck, test:unit, build) passes clean afterward.

## 8. Documentation

- [x] 8.1 Write the project README, and verify every command it documents actually runs
    - **Implements:** Constitution principle 9
    - **Files:** `README.md`
    - **Details:** Follow the sibling project's shape: intro with the explicit negative-scope statement (no backend, API, account or database), prerequisites including the Node floor, a table mapping every npm script to its purpose, the local gate block, a fenced repository-layout listing with one line per directory, the current implementation status (Phase 1 complete; Phases 2–11 pending), and the licence note covering both the MIT application licence and the SIL OFL fonts with a link to `src/assets/fonts/README.md`.
    - **Note:** `master` has no implementation yet (verified via `git log master`), so the README omits the sibling's "play the deployed game" link — nothing is live to link to. All 16 `package.json` scripts are documented, not just the subset the sibling table covers, since this task's verify step requires every command to actually run.
    - **Verify:** Every command in the script table runs successfully from a clean checkout — confirmed: `format`, `format:check`, `lint`, `lint:fix`, `typecheck`, `build`, `test`, `test:unit`, `test:coverage` and `prepare` all exit 0; `dev`, `dev-network` and `preview` each serve `/solitaire/` with a `200` response when started and stopped; `e2e` passes 6/6 (Firefox required the same `CFFIXED_USER_HOME` macOS-27 workaround as task 5.1); `rtk npm run validate` passes clean afterward. `rtk npm run format:check` passes on the README — confirmed.

- [x] 8.2 Reconcile the documents invalidated by this change, and verify no document still describes the former rules
    - **Implements:** Constitution principle 9; design decision D1
    - **Files:** `AGENTS.md`, `docs/spec/phased-design.md` (§1 "Quality gates" row), `openspec/config.yaml` (`context` block)
    - **Details:** Four corrections. (a) **Semicolons:** `phased-design.md` §1, `AGENTS.md` and the `openspec/config.yaml` `context` block all say "no semicolons" — change all three to state semicolons, 4 spaces, 120 columns, single quotes, trailing commas. The config block matters most: it is injected into every future planning session. (b) **Scaffolding status:** `AGENTS.md` says the repository is pre-implementation with no `package.json` or tooling — replace with the real script list, the hook gates, and the Phase 8 / Phase 4 deferrals of `validate:artifact` and `validate:lifecycle-storage`. (c) **Speckit:** `AGENTS.md` says Speckit "will be installed later" and instructs copying the constitution into `.specify/memory/constitution.md` — OpenSpec is installed and Speckit is not, so state that and drop the `.specify` instruction, keeping `AGENTS.md` itself as the constitution's home. (d) **Divergence from the sibling project:** record that semicolons, git hooks and lint strictness now deliberately differ from `sanyokkua/minesweeper`, so "as in Minesweeper" is no longer literal for those three.
    - **Note:** during implementation, `docs/spec/README.md` was found to carry the same class of stale reference (`specify init`, `/speckit.*` steps, and a `/speckit.plan` mention in "Authority order") pointing at the same superseded Speckit workflow AGENTS.md:77 referenced — outside this task's stated file list. Surfaced to the user as added scope rather than silently absorbed; the user explicitly approved folding it in, so `docs/spec/README.md` was corrected as a fourth file alongside the three listed above.
    - **Verify:** `grep -rin "no semicolon\|no-semicolons" AGENTS.md docs/spec openspec/config.yaml README.md` returns nothing — confirmed; `grep -n "not set up yet\|will be installed into this repo later" AGENTS.md` returns nothing — confirmed; `grep -n "specify init\|/speckit\." docs/spec/README.md` returns nothing — confirmed; `rtk npm run validate` still passes — confirmed (format:check, lint, typecheck, 142/142 unit tests, build all green)

## 9. Whole-change verification

- [x] 9.1 Run the complete gate end to end on a clean checkout and confirm every spec scenario is covered by a passing check
    - **Implements:** T "Single aggregate validation gate"
    - **Details:** Spans every task above, so it is a separate verification task rather than an inline one. Walk each `#### Scenario:` in both delta specs and name the test or command that exercises it; anything unexercised is a gap to close, not to note.
    - **Files:** `tests/unit/repo/reservedLayers.test.ts` (new), `tests/unit/ui/tokens.test.ts` (extended), `tests/unit/repo/configContract.test.ts` (extended)
    - **Note — gaps closed:** four scenarios had no exercising check at all (not even a one-off manual probe from an earlier task); each now has a permanent automated test. (1) app "Reserved layer contains no code" — task 4.5 only ran a one-off `find`; new `tests/unit/repo/reservedLayers.test.ts` asserts `src/{domain,solver,features,i18n,pwa}` each contain only `README.md`. (2) app "Licences are recorded" — `src/assets/fonts/README.md` existed but nothing read it; `tokens.test.ts` gained a test asserting it documents a licence for both bundled font files. (3) app "Components do not hard-code colour" — no check existed; `tokens.test.ts` gained a test asserting `global.css` contains no hex/`rgb()`/`hsl()` literal outside `var(--...)` (allowing `transparent`/`currentColor`/`none`). (4) tooling "Remote asset reference is rejected" for the HTML entry point — `tokens.test.ts` only ever covered `tokens.css`; `index.html` was only grepped manually against the *built* `dist/` output in task 1.3, never checked at the source level as a repeatable test; `configContract.test.ts` gained a describe block asserting source `index.html` contains no `http://`/`https://` reference. All three files pass under `rtk npm run test:unit` (151/151) and `rtk npm run validate`.
    - **Note — scenarios spec'd but never probed, probed now (reverted, not permanent):** (a) tooling "Unsupported Node version" — temporarily raised `engines.node` past the installed version and ran `rtk npm ci`: confirmed npm reports `npm warn EBADENGINE Unsupported engine` (not silent) and still completes (default npm behaviour, no `.npmrc` `engine-strict`), then reverted. (b) tooling "Build blocked by a type error" — task 1.2 only probed `typecheck` directly; temporarily introduced a type error and ran `rtk npm run build` (`tsc -b && vite build`) itself: confirmed non-zero exit and `dist/index.html`'s mtime unchanged, then reverted. (c) tooling "Gate stops at the first failure" — never isolated before; temporarily introduced a lint violation and ran `rtk npm run validate`: confirmed `format:check` ran and passed, `lint` failed and the run stopped there (no typecheck/test/build output), then reverted. (d) tooling "Failing unit test blocks the commit" — task 6.1 only probed the format-fix and type-error paths; temporarily broke an assertion in `tests/unit/app/store.test.ts`, staged and committed: confirmed husky's pre-commit hook failed at the `test:unit` step (`husky - pre-commit script failed (code 1)`) and HEAD stayed unchanged, then reverted and unstaged.
    - **Note — full scenario → check mapping.** `tooling/repository-foundation`: Clean install from a fresh checkout → `rtk npm ci` on a clean-checkout copy (below), lockfile byte-identical before/after. Unsupported Node version → fresh probe (b) above. Successful build → `rtk npm run build` (task 1.3, reconfirmed in the clean-checkout run). Build blocked by a type error → fresh probe (b) above. Conformant tree passes the check → `rtk npm run format:check` (reconfirmed). Non-conformant file fails the check → task 2.1's probe. Specification pack is left alone → `.prettierignore` + task 2.1's `git status --porcelain docs/ openspec/` check. Clean tree lints cleanly → `rtk npm run lint` (reconfirmed). Explicit `any` is rejected / Unhandled promise is rejected → task 2.2's probes. Unchecked index access is rejected → task 1.2's probe. In-process layer excludes end-to-end specs → `rtk npm run test:unit` (`vitest.config.ts` `exclude`, reconfirmed). Browser layer covers desktop and touch → `rtk npm run e2e`, all 6 projects (reconfirmed in the clean-checkout run). Coverage falls below threshold → task 7.4's `describe.skip` probe. Whole gate passes → `rtk npm run validate` (reconfirmed, real repo and clean-checkout copy). Gate stops at the first failure → fresh probe (c) above. Mis-formatted staged file is corrected in place → task 6.1's probe. Type error blocks the commit → task 6.1's probe. Failing unit test blocks the commit → fresh probe (d) above. Failing end-to-end test blocks the push → task 6.2's probe, reconfirmed via a fresh throwaway-bare-repo push in this task (broken assertion rejected, `husky - pre-push script failed`; restored assertion pushed successfully). Pull request is validated / Failure preserves evidence → **out of reach**, see below. Default-branch push deploys / Non-default branch does not deploy → the ref guard is asserted by `configContract.test.ts`; the live deploy itself is **out of reach**, see below. Elevated permissions are scoped → `configContract.test.ts`'s `pages.yml` deploy-job-isolation tests. One configuration drifts → `configContract.test.ts`'s base-path `it.each`, and task 7.3's drift probe. Remote asset reference is rejected → `tokens.test.ts` (stylesheets) plus the new `configContract.test.ts` `index.html` check (gap 4 above). `app/application-shell`: First load shows Home / Exactly one screen at a time → `tests/component/appShell.test.tsx` (the single `route === 'home' ? ... : ...` ternary in `App.tsx` guarantees exclusivity; the button-presence assertions are sufficient proxy evidence since each screen's only unique content is its heading and button). Pointer/Keyboard navigation to Game, Return to Home → `appShell.test.tsx`. Focus is visible → `global.css`'s `:focus-visible` rule (visual/structural; not asserted by an automated check — see below). Every role is defined in every palette / Dark palette applies without a system change (via the `:root[data-theme='dark']` attribute-selector structure, not a `prefers-color-scheme` media query) / Fonts resolve from the bundle → `tokens.test.ts`. Components do not hard-code colour / Licences are recorded → new `tokens.test.ts` checks (gaps 2–3 above). Production build shows its timestamp / Development build shows a placeholder → `tests/component/buildStamp.test.tsx`. Reserved layer contains no code → new `reservedLayers.test.ts` (gap 1 above).
    - **Note — scenarios that stay out of reach.** "Pull request is validated," "Failure preserves evidence" and "Default-branch push deploys" (live GitHub Pages deploy) can only be exercised by an actual push/PR/merge to `master` on GitHub, which is outside this task's reach under `AGENTS.md`'s branch policy — task 7.2 already recorded the same limitation for its own scenarios as a follow-up; restated here rather than silently passed over. "Focus is visible" has no automated check: it is a visual/CSS fact (`global.css`'s `button:focus-visible { outline: 3px solid var(--color-primary); ... }`) that the existing component and e2e suites don't assert against computed style; left as a manual/visual fact rather than fabricating a brittle computed-style test, consistent with this change's scope (no visual-regression tooling exists yet).
    - **Verify:** Clean checkout — copied the working tree (respecting `.gitignore`) into a scratch directory with no `node_modules`/`dist`/`coverage`: `npm ci` completed with the lockfile byte-identical (same SHA-1 before/after) — confirmed; `npm run validate` exited 0 (151/151 tests, clean build) — confirmed; `npm run e2e` passed all 6 projects — confirmed. Push-time gate reconfirmed against a local throwaway bare repo (never `origin`): broken e2e assertion → push rejected by `husky - pre-push script failed`; restored assertion → push succeeded, 6/6 projects passing. Commit-time gate reconfirmed via fresh probe (d) above. `rtk npm run validate` passes clean in the real repo afterward — confirmed.
